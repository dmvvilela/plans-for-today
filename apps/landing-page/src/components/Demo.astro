---

---

<section class="demo" id="demo">
  <div class="container">
    <div class="section-header">
      <h2>See it in <span class="handwritten">action</span></h2>
      <p>A whiteboard that works the way you think</p>
    </div>

    <div class="demo-content">
      <div class="demo-board">
        <div class="board-header">
          <div class="board-title handwritten">Plans For Today</div>
          <div class="board-date handwritten">December 22, 2024</div>
        </div>

        <div class="board-items">
          <div class="board-item">
            <span class="handwritten item-text">✓ Morning meditation</span>
            <div class="strike-line"></div>
          </div>
          <div class="board-item">
            <span class="handwritten item-text">✓ Review project proposals</span>
            <div class="strike-line"></div>
          </div>
          <div class="board-item">
            <span class="handwritten item-text">Call Mom for her birthday</span>
          </div>
          <div class="board-item">
            <span class="handwritten item-text">Gym - leg day</span>
          </div>
          <div class="board-item">
            <span class="handwritten item-text">Prep tomorrow's presentation</span>
          </div>
        </div>

        <div class="board-doodle">
          <svg width="200" height="100" viewBox="0 0 200 100">
            <path d="M 20 50 Q 100 20 180 50" stroke="var(--orange)" stroke-width="3" fill="none" stroke-linecap="round" opacity="0.6"/>
            <circle cx="180" cy="50" r="8" fill="var(--orange)" opacity="0.6"/>
          </svg>
        </div>
      </div>

      <div class="demo-features">
        <div class="demo-feature">
          <div class="demo-number">1</div>
          <div class="demo-text">
            <h4>Just start typing</h4>
            <p>Click anywhere and type. Your text appears instantly in beautiful handwriting.</p>
          </div>
        </div>

        <div class="demo-feature">
          <div class="demo-number">2</div>
          <div class="demo-text">
            <h4>Natural gestures</h4>
            <p>Strike through to complete. Scribble to delete. Draw to annotate.</p>
          </div>
        </div>

        <div class="demo-feature">
          <div class="demo-number">3</div>
          <div class="demo-text">
            <h4>Daily persistence</h4>
            <p>Each day automatically saved. Navigate through your history anytime.</p>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .demo {
    padding: 5rem 2rem;
    background: linear-gradient(180deg, white 0%, var(--whiteboard) 100%);
  }

  .container {
    max-width: 1200px;
    margin: 0 auto;
  }

  .section-header {
    text-align: center;
    margin-bottom: 4rem;
  }

  .section-header h2 {
    font-size: clamp(2rem, 4vw, 3rem);
    font-weight: 700;
    color: var(--dark-blue);
    margin-bottom: 1rem;
  }

  .section-header h2 span {
    color: var(--green);
    font-size: 1.2em;
    transform: rotate(-2deg);
    display: inline-block;
  }

  .section-header p {
    font-size: 1.2rem;
    color: #666;
  }

  .demo-content {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 4rem;
    align-items: center;
  }

  .demo-board {
    background: white;
    border: 3px solid var(--blue-border);
    border-radius: 20px;
    padding: 2rem;
    box-shadow:
      0 0 0 2px var(--dark-blue),
      0 8px 32px rgba(0, 0, 0, 0.1);
    position: relative;
    transform: rotate(-1deg);
  }

  .board-header {
    margin-bottom: 2rem;
  }

  .board-title {
    font-size: 2.5rem;
    font-weight: 700;
    color: var(--dark-blue);
    margin-bottom: 0.5rem;
  }

  .board-date {
    font-size: 1.2rem;
    color: #666;
    opacity: 0.7;
  }

  .board-items {
    display: flex;
    flex-direction: column;
    gap: 1rem;
  }

  .board-item {
    position: relative;
    font-size: 1.5rem;
    color: var(--dark-blue);
    padding: 0.5rem 0;
  }

  .strike-line {
    position: absolute;
    top: 50%;
    left: -10px;
    right: -10px;
    height: 3px;
    background: var(--dark-blue);
    opacity: 0.4;
    transform: translateY(-50%) rotate(-1deg);
  }

  .board-doodle {
    position: absolute;
    bottom: 2rem;
    right: 2rem;
    opacity: 0.6;
  }

  .demo-features {
    display: flex;
    flex-direction: column;
    gap: 2rem;
  }

  .demo-feature {
    display: flex;
    gap: 1.5rem;
    align-items: flex-start;
  }

  .demo-number {
    width: 48px;
    height: 48px;
    background: var(--orange);
    color: white;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 1.5rem;
    font-weight: 700;
    flex-shrink: 0;
  }

  .demo-text h4 {
    font-size: 1.4rem;
    color: var(--dark-blue);
    margin-bottom: 0.5rem;
  }

  .demo-text p {
    color: #666;
    line-height: 1.6;
  }

  @media (max-width: 968px) {
    .demo-content {
      grid-template-columns: 1fr;
    }

    .demo-board {
      max-width: 600px;
      margin: 0 auto 3rem;
    }
  }
</style>